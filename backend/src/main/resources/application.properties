# Database Configuration
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:expensetracker}
spring.datasource.username=${DB_USER:expensetracker}
spring.datasource.password=${DB_PASSWORD:7303290820}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# H2 console (disabled in production)
spring.h2.console.enabled=${H2_CONSOLE_ENABLED:false}
spring.h2.console.path=/h2-console

# JWT Configuration
app.jwt.secret=${JWT_SECRET:Y8s2LxX7oH4x1Jw2PzQ7iV9sT3qBm0cN6rU4aV2yZ5nK8dQ1tG6hJ3lW8mR2cF9a}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# Server Configuration
server.port=${SERVER_PORT:8080}

# Logging
logging.level.com.example.expensetracker=INFO
logging.level.org.springframework.security=INFO
